# Dynamic Query Widgets - HomeFinder

Dynamic querying - The notion of dynamic queries to continuously update the data that is filtered from the database and visualized. These queries ideally work instantly as the user adjusts UI controls such as sliders or dropdowns to form simple queries or to find patterns or exceptions. This approach was evaluated using real estate system called HomeFinder.
